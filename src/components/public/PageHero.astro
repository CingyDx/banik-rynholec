---
type Props = {
  title: string;
  description: string;
};

const { title, description } = Astro.props;
---

<section class="page-hero">
  <div class="container">
    <h1>{title}</h1>
    <div class="section-rule"></div>
    <p>{description}</p>
  </div>
</section>

